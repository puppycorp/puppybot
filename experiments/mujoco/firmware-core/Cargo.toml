[package]
name = "puppybot-mujoco-firmware-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"

[lib]
crate-type = ["cdylib"]

[dependencies]
puppybot-core = { path = "../../../puppybot/core", default-features = false, features = ["runtime"] }

[workspace]
